{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C Project",
            "type": "shell",
            "command": "powershell -Command \"gcc -I Lessons @(Get-ChildItem -Recurse -Filter *.c).FullName -o learningc\"",
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"]
        }
    ]
}